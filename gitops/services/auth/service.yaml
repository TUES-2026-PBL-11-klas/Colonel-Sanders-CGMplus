apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: services
  labels:
    app: auth
spec:
  selector:
    app: auth
  ports:
    - name: http
      port: 5000
      targetPort: 5000
