apiVersion: v1
kind: Service
metadata:
  name: auth-postgres
  namespace: services
  labels:
    app: auth-postgres
spec:
  selector:
    app: auth-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
